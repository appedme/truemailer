import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#1677ff',
    borderRadius: 6,
  },
  components: {
    Button: {
      borderRadius: 6,
    },
  },
};

export default theme;
